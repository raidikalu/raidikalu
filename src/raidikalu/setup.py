
from setuptools import setup, find_packages

setup(
    name='raidikalu',
    version='0.1',
    license='raidikalu',
    description='raidikalu',
    author='raidikalu',
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    setup_requires=[],
    install_requires=[],
)
